import { StyleSheet, Text, View } from "react-native";

export const gStyle = StyleSheet.create({
  main: {
    padding: 20,
    paddingTop: 60,
  },
  title: {
    fontSize: 25,
    color: "#333",
    textAlign: "center",
    marginTop: 25,
  },
});
